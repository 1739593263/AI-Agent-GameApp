spring:
  application:
    name: PolyModal-ai-agent
  profiles:
    active: local
#  datasource:
#    url: jdbc:postgresql://address:5432/db
#    username: XXX
#    password: XXX
#    ai:
#      vectorstore:
#        pgvector:
#          index-type: HNSW
#          distance-type: COSINE_DISTANCE
#          dimensions: 1024
#          max-document-batch-size: 10000 # Optional: Maximum number of documents per batch
#      dashscope:
#        api-key: "XXX"
#        chat:
#          options:
#            model: qwen-max
#      mcp:
#        client:
#          enabled: true
#          type: sync
#          sse:
#          stdio:

server:
  port: 8023
  servlet:
    context-path: /api

# springdoc-openapi????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.springai.polymodalaiagent.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn
logging:
  level:
    org.springframework.ai.chat.client.advisor.SimpleLoggerAdvisor: debug
